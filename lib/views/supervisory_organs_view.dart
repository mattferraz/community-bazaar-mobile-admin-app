import 'package:flutter/material.dart';

class SupervisoryOrgansView extends StatelessWidget {
  const SupervisoryOrgansView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}